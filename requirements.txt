# Django web app requirements for JWQL project — install-safe pins for Python 3.12 and cross-platform compatibility.
# Do NOT include jwedb or problematic packages. Last updated to fix import errors (jsonschema, etc.)

Django>=3.2,<5.0
djangorestframework>=3.12
numpy>=1.21
pandas>=1.4
jsonschema>=4.0    # Explicit (required by jwql/utils/utils.py)
requests>=2.28
pyyaml>=6.0
astropy>=5.0
bokeh>=2.4
matplotlib>=3.4
pillow>=9.0
scipy>=1.8
# Database & migration support
psycopg2-binary>=2.9
sqlalchemy>=1.4
alembic>=1.7
# File handling & misc
beautifulsoup4>=4.10
lxml>=4.8
tqdm>=4.64
# Any additional requirements used in utils or main app (leave commented for optional install)
# tqdm
# openpyxl
# scikit-image
# h5py
# The following packages are intentionally excluded due to prior install failures or deprecation:
# jwedb

# Optional visualization libraries
seaborn>=0.11
plotly>=5.6

# Extra dependencies (if any) can be added below as needed by new features.
