# Django web app requirements for JWQL project — install-safe pins for Python 3.12 and cross-platform compatibility.
# Do NOT include jwedb or problematic packages. Last updated to fix import errors (jsonschema, etc.)

Django>=3.2,<5.0
djangorestframework>=3.12

# Core scientific stack
numpy>=1.24.0,<2.0.0
pandas>=1.5.0,<3.0.0
astropy>=5.0.0,<7.0.0
astroquery>=0.4.6,<0.5.0

# Util & validation deps
inflection>=0.5.1,<0.6.0
jsonschema>=4.0.0,<5.0.0
requests>=2.28.0,<3.0.0
PyYAML>=6.0.0,<7.0.0

# AWS and Cloud (used in monitor_utils etc)
boto3>=1.26.0,<2.0.0

# Visualization and plotting
bokeh>=2.4
matplotlib>=3.4
seaborn>=0.11
plotly>=5.6
pillow>=9.0

# Analysis and convenience tools
scipy>=1.8
tqdm>=4.64
beautifulsoup4>=4.10
lxml>=4.8

# Files and database
psycopg2-binary>=2.9
sqlalchemy>=1.4
alembic>=1.7

# The following packages are intentionally excluded due to prior install failures or deprecation:
# jwedb

# Extra dependencies (if any) can be added below as needed by new features.
